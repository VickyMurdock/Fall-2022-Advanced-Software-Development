from django.apps import AppConfig


class GoogleauthenticationConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "googleauthentication"

class TimeTableConfig(AppConfig):
    name = "TimeTable"
